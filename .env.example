# External MariaDB / MySQL connection.
# The app creates the database (DB_NAME) and all tables on first start,
# so the DB user needs CREATE DATABASE permission (or pre-create DB_NAME
# and grant the user rights on it).
DB_HOST=192.168.1.10
DB_PORT=3306
DB_USER=tracker
DB_PASSWORD=changeme
DB_NAME=transition_tracker
