"""Deprecated.

Financial aggregates (yearly Budget/Actual/Forecast/Total, currency conversion,
leg and year totals) are now computed client-side from the raw monthly data
returned by GET /financial-years/{id}/view. This module is intentionally empty.
"""
